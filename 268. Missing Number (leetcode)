 int missingNumber(vector<int>& nums)
    {
        int temp = (nums.size() * (nums.size()+1)) / 2;   
        int sum = 0;

        for(int i = 0; i < nums.size(); i++)
        {
            sum = sum + nums[i];
        } 

        return temp - sum;
    }
